# fetchConfig parses camelCase keys (gatingMode, flagsRequireReason, maxDebateRounds).
# Snake_case keys (gating_mode) are silently ignored and fall back to defaults.
gatingMode: hard_block
flagsRequireReason: true
maxDebateRounds: 3
